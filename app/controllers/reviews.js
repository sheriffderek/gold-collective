import Ember from 'ember';

export default Ember.Controller.extend({



  // sortProperties: ['id:asc'],
  // randomReviews: Ember.computed.sort('model.reviews', function(a) {
  //   return a;
  // }),

});
