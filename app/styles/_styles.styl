
// $components
figure, .image-w
	responsive-image-wrapper()

svg
	fill: currentColor

// $styles
body > .ember-view // master wrapper by default
	border: 3px solid blue
	display: flex
	flex-direction: column
	min-height: 100vh

.application-outlet
	display: flex
	flex-direction: column
	flex-grow: 1

.legal
	// border: 3px solid green

body.landing
	//
	.application-outlet
		justify-content: center
		align-items: center
	.site-header
		display: none
	.image-slider
		position: fixed
		top: 0
		right: 0
		bottom: 0
		left: 0
		width: 100%
		height: 100%
		z-index: 0
		.image
			height: 100%
			background-size: cover
			background-position: center center
	.portal
		position: relative
		display: block
		width: 80vw
		z-index: 5
		color: white
		// g
		// 	&:hover
		// 		color: black

	.legal
		display: none

body.about
	//

body.faqs
	//

body.investment
	//

body.reviews
	//

body.contact
	//

body.inspiration
	//
